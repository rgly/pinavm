# This is the helper shell script for users who wants to 
# compile LLVM bitcode manually. Normally using CMake buildBitcode()
# is more convenient.

export LLVMC_FLAGS="@LLVMC_FLAGS_SPACE@"
export LLVM_COMPILER=@LLVM_COMPILER@
export LLVM_PATH=@LLVM_ROOT@/bin
export BINARY_ROOT=@CMAKE_BINARY_DIR@
